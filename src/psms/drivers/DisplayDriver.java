package psms.drivers;

/**
 * Display Driver (SAD Section 2.3)
 * Translates presentation commands from Display Manager into hardware protocols
 * for the digital screens. Maintains an internal buffer for visual continuity.
 */
public class DisplayDriver {
    public void writeToDisplay(String deviceId, String data) {
        // Writes data to physical display hardware
    }
}
